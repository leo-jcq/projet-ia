/**
 * Les modes du bot.
 * @enum {string}
 */
enum Mode {
    /**
     * Mode visuel : détaille les étapes.
     */
    Visual = 'visual',

    /**
     * Mode performance : aucun affichage, permet une mesure plus correcte des performances.
     */
    Performance = 'perf'
}

export default Mode;
